var user = require('../../../../src/main/js/models/user');

describe('User', function () {
  describe('trying to test something', function () {
    it('should do something interesting', function () {
      console.log('user testing... [' + user.name + ']');
    });
  });
});