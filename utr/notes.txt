Todo for BRD

* Refine definitions for
  - accounts setup (UAF)
  - transaction for each accounts (set up of client ref # in each transactions)
  - transA (containing client ref #) -> transB (containing txA #) -> transC etc

* Document/expan the condition that initiates each messages

* Break up UTR component

